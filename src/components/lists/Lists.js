import ListsList from "./ListsList"
import ListsMenu from "./ListsMenu"

const Lists = () => (
    <div className="lists">
        <ListsMenu />
        <ListsList />
    </div>
)

export default Lists